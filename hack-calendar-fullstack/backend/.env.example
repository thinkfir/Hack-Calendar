# Server Configuration
PORT=3001

# Database Configuration
# Leave empty for in-memory, or provide a path e.g., ./database.sqlite
DB_PATH=

# JWT Configuration
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=1d

# Master AI Configuration (for the built-in provider)
# Example: 'openai', 'groq', etc.
MASTER_AI_PROVIDER=
MASTER_API_KEY=